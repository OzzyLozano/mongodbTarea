install dependencies:
  npm init
  npm install express --save
  npm install -g nodemon
  npm install mongodb --save

run app:
  nodemon app.js

git commands:
  git add .
  git commit -m "<msg>"
  git push
  git pull
